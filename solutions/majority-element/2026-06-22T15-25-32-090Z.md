# Majority Element

- LeetCode URL: https://leetcode.com/problems/majority-element/submissions/2042336565/
- Status: Accepted
- Language: Java
- Timestamp: 2026-06-22T15:25:32.090Z

## Code

```java
) {
        HashMap<Integer, Integer> map = new HashMap<>();
        HashSet<Integer> set = new HashSet<>();

        for (int i=0; i<nums.length; i++) {
            int sum = 0;
            if (!set.contains(nums[i])) {
                set.add(nums[i]);
                map.put(nums[i], 1);
            } else {
                int currentCount = map.get(nums[i]);
                map.put(nums[i], currentCount + 1);
            }
        }

        int highestValue = Collections.max(map.values());

        for (int i : map.keySet()) {
            if (map.get(i) == highestValue) {
                return i;
            }
        }

        return nums[0];

    }
}
```
