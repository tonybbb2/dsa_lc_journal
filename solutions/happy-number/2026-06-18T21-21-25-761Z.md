# Why the input number cannot go to infinity (an explanation/proof that fits the context of a technical interview)

- LeetCode URL: https://leetcode.com/problems/happy-number/submissions/2038042491/?envType=problem-list-v2&envId=hash-table
- Status: Accepted
- Language: Java
- Timestamp: 2026-06-18T21:21:25.761Z

## Code

```java
mber as seen

        for (int i = 0; i < size; i++) {
            char ch = str.charAt(i);
            int num = Integer.parseInt(String.valueOf(ch));

            map.put(i, num * num); 
        }

        if (map.size() >= 1) { 
            int[] listVal = map.values()
                .stream()
                .mapToInt(Integer::intValue)
                .toArray();

            for (int val : listVal) {
                sum += val;
            }
        }

        return isHappy(sum);
    }
}


// class Solution {
//     public boolean isHappy(int n) {
//         boolean result = false;
//         String str = String.valueOf(n);
//         int size = str.length();

//         HashMap<Integer, Integer> map = new HashMap<>();

//         int sum = 0;

//         if (n == 1) {
//             return true;
//         }

//         for (int i =0; i<size; i++) {
//             char ch = str.charAt(i);
//             int num = Integer.parseInt(String.valueOf(ch));

//             map.put(i,
```
