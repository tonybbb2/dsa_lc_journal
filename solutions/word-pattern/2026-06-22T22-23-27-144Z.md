# Word Pattern

- LeetCode URL: https://leetcode.com/problems/word-pattern/submissions/2042691256/
- Status: Accepted
- Language: Java
- Timestamp: 2026-06-22T22:23:27.144Z

## Code

```java
ter, String> map = new HashMap<>();
        HashSet<String> set = new HashSet<>();
        
        if (list.length != pattern.length()) {
            return false;
        }

        for (int i=0; i<list.length; i++) {
            if (!map.containsKey(pattern.charAt(i))) {
                if (set.contains(list[i])){
                    return false;
                }
                set.add(list[i]);
                map.put(pattern.charAt(i), list[i]);
            } else {
                if (!list[i].equals(map.get(pattern.charAt(i)))) {
                    samePattern = false;
                }
            }
        }

        System.out.println(map);

        return samePattern;
    }
}
```
