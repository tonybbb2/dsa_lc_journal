# Sort Characters By Frequency

- LeetCode URL: https://leetcode.com/problems/sort-characters-by-frequency/submissions/2045815452/
- Status: Accepted
- Language: Java
- Timestamp: 2026-06-25T13:12:42.747Z

## Code

```java
if (entry.getValue() == maxValue) { // On cherche la clé qui a la valeur max
                        key = entry.getKey();
                        break;
                    }
                }

                for (int j = 0; j < maxValue; j++) {
                    result += key; // On peut aussi utiliser un StringBuilder pour optimiser
                }

                map.remove(key);
            }
           
    
        System.out.println(result);

        return result;
    }
}
```
