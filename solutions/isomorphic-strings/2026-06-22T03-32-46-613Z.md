# Isomorphic Strings

- LeetCode URL: https://leetcode.com/problems/isomorphic-strings/submissions/2041551047/?envType=problem-list-v2&envId=hash-table
- Status: Accepted
- Language: Java
- Timestamp: 2026-06-22T03:32:46.613Z

## Code

```java
ngth() == 1 && s.charAt(0) == t.charAt(0)) {
            return true;
        }

        for (int i=0; i<s.length();i++) {
            if (!set.contains(s.charAt(i))) {
                set.add(s.charAt(i));
                map.put(s.charAt(i), t.charAt(i));
            } else if (set.contains(s.charAt(i))) {
                char value = map.get(s.charAt(i));
                if (value == t.charAt(i)) {
                    result = true;
                } else {
                    result = false;
                }
            }
        }

                boolean allUnique = map.values().stream().distinct().count() == map.size();
                if (allUnique) {
                    result = true;
                }

        return result;

    }
}
```
