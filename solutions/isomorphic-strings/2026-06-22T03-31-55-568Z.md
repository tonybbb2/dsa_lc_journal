# Isomorphic Strings

- LeetCode URL: https://leetcode.com/problems/isomorphic-strings/submissions/2041550442/?envType=problem-list-v2&envId=hash-table
- Status: Accepted
- Language: Java
- Timestamp: 2026-06-22T03:31:55.568Z

## Code

```java
ns(s.charAt(i))) {
                set.add(s.charAt(i));
                map.put(s.charAt(i), t.charAt(i));
            } else if (set.contains(s.charAt(i))) {
                char value = map.get(s.charAt(i));
                if (value == t.charAt(i)) {
                    result = true;
                }
            }

                boolean allUnique = map.values().stream().distinct().count() == map.size();
                if (allUnique) {
                    result = true;
                }

        return result;

    }
}
```
