# Isomorphic Strings

- LeetCode URL: https://leetcode.com/problems/isomorphic-strings/submissions/2041558659/?envType=problem-list-v2&envId=hash-table
- Status: Accepted
- Language: Java
- Timestamp: 2026-06-22T03:42:50.936Z

## Code

```java
ap = new HashMap<>();
        HashSet<Character> set = new HashSet<>();

        boolean result = true;

        if (s.length() != t.length()) {
            return false;
        } 

        if (s.length() == 1 && s.charAt(0) == t.charAt(0)) {
            return true;
        }

        for (int i=0; i<s.length();i++) {
            if (!set.contains(s.charAt(i)) && !map.containsValue(t.charAt(i))) {
                set.add(s.charAt(i));
                map.putIfAbsent(s.charAt(i), t.charAt(i));

                // if () {
                //     result = true;
                // }
            } else if (set.contains(s.charAt(i))) {
                char value = map.get(s.charAt(i));
                System.out.println(value);
                System.out.println(t.charAt(i));
                if (value != t.charAt(i)) {
                    result = false;
                } else {
                    result = true;
                }
            }
        }

        return result;

    }
}
```
