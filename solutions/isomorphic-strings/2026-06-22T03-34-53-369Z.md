# Isomorphic Strings

- LeetCode URL: https://leetcode.com/problems/isomorphic-strings/submissions/2041552561/?envType=problem-list-v2&envId=hash-table
- Status: Accepted
- Language: Java
- Timestamp: 2026-06-22T03:34:53.369Z

## Code

```java
r (int i=0; i<s.length();i++) {
            if (!set.contains(s.charAt(i))) {
                set.add(s.charAt(i));
                map.put(s.charAt(i), t.charAt(i));
            } else if (set.contains(s.charAt(i))) {
                char value = map.get(s.charAt(i));
                System.out.println(value);
                System.out.println(t.charAt(i));
                if (value != t.charAt(i)) {
                    result = false;
                } else {
                    result = true;
                }
            }
        }

                boolean allUnique = map.values().stream().distinct().count() == map.size();
                if (allUnique) {
                    result = true;
                }

        return result;

    }
}
```
