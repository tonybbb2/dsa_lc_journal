# Isomorphic Strings

- LeetCode URL: https://leetcode.com/problems/isomorphic-strings/?envType=problem-list-v2&envId=hash-table
- Status: Accepted
- Language: Java
- Timestamp: 2026-06-22T03:41:15.972Z

## Code

```java
class Solution {
    public boolean isIsomorphic(String s, String t) {
        HashMap<Character, Character> map = new HashMap<>();
        HashSet<Character> set = new HashSet<>();

        boolean result = false;

        if (s.length() != t.length()) {
            return false;
        } 

        if (s.length() == 1 && s.charAt(0) == t.charAt(0)) {
            return true;
        }

        for (int i=0; i<s.length();i++) {
            if (!set.contains(s.charAt(i)) && !map.containsValue(t.charAt(i))) {
                set.add(s.charAt(i));
                map.putIfAbsent(s.charAt(i), t.charAt(i));
                // result = true;
            } else if (set.contains(s.charAt(i))) {
                char value = map.get(s.charAt(i));
                System.out.println(value);
                System.out.println(t.charAt(i));
                if (value != t.charAt(i)) {
                    result = false;
```
