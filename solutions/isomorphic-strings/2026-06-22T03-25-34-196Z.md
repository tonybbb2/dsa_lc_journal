# Isomorphic Strings

- LeetCode URL: https://leetcode.com/problems/isomorphic-strings/submissions/2041546173/?envType=problem-list-v2&envId=hash-table
- Status: Wrong Answer
- Language: Java
- Timestamp: 2026-06-22T03:25:34.196Z

## Code

```java
if (s.length() != t.length()) {
            return false;
        }

        for (int i=0; i<s.length();i++) {
            if (!set.contains(s.charAt(i))) {
                set.add(s.charAt(i));
                map.put(s.charAt(i), t.charAt(i));
            } else if (set.contains(s.charAt(i))) {
                char value = map.get(s.charAt(i));
                if (value == t.charAt(i)) {
                    result = true;
                }
            } else {
                result = false;
            }
        }

        return result;

    }
}
```
